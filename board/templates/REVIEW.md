# 验收报告

验收端: <端名>/<模型名>
验收结果: PASS
合同指纹: <从 card.md / status.json 读取 contract_fingerprint>
问题数: 0

> 字段行必须顶格写，不要加列表前缀（`- `）。`验收结果` 只接受 `PASS` 或 `FAIL`。
> 验收端必须是非执行方的注册端；L3/L4 还要求不同模型家族。

## 复测

- 对照 `card.md` / `contract.json` 的验收标准逐条复测。
- 核对 `PROGRESS.md` 署名与产物路径。
- 记录实际命令与结果。

## 问题清单

（FAIL 时必填；PASS 可写「无」。）

## 改进建议

非阻断建议写在这里。存在影响冻结合同验收的问题必须改判 FAIL，不要另设第三种结论。
